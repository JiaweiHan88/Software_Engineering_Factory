# Configuration — Placeholder

Runtime configuration files and loaders.

Planned configuration (Phase 3):

- `config.ts` — Runtime config loader (reads env vars + .env)
- Model selection, Paperclip URL, heartbeat intervals
- Agent-to-model mapping overrides
