# MCP Servers — Placeholder

This directory will contain MCP (Model Context Protocol) server definitions
for exposing external tools to the Copilot SDK agents.

Planned MCP servers (Phase 5):

- **File System MCP** — Safe file read/write for story artifacts
- **Git MCP** — Branch, commit, PR management
- **Sprint Status MCP** — Read/write sprint-status.yaml
- **Notification MCP** — Slack/webhook notifications for story lifecycle events
